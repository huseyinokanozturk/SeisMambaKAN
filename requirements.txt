# --- PyTorch Core (Already available in Colab but good to pin version) ---
torch>=2.0.0

# --- Model Architecture (Mamba & KAN) ---
# Required for Mamba GPU kernels
causal-conv1d>=1.4.0
mamba-ssm>=2.2.0
# KAN layers implementation
efficient-kan

# --- Data Loading (Dataset Management) ---
webdataset
pyyaml
numpy
pandas

# --- Training Monitoring & Progress Tracking ---
tensorboard
tqdm
matplotlib