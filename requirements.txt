# --- Core ML dependencies (torch is handled in run.py setup / colab_setup.py) ---
# --- Model Architecture (Mamba & KAN) ---

# Required for Mamba GPU kernels
#mamba-ssm[causal-conv1d]>=2.2.0 --> using pre-build wheels for now

# KAN layers implementation
efficient-kan @ git+https://github.com/Blealtan/efficient-kan.git

# mamba_ssm 2.2.x pulls in `transformers` and imports
# `GreedySearchDecoderOnlyOutput` from `transformers.generation`,
# which was removed in transformers >= 4.45. Pin to the last range
# that exports it.
transformers<4.45

# --- Data Loading (Dataset Management) ---
webdataset
pyyaml
numpy
pandas
h5py
scikit-learn

# --- Training Monitoring & Progress Tracking ---
tensorboard
tqdm
matplotlib

# --- CLI (run.py) ---
typer>=0.12
rich>=13.7
